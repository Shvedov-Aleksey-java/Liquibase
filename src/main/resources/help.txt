создаем миграцию с помощью liquibase


//данный файл отвечает за конфигурацию именно в нем мы подключаем файлы для миграции
db.changelog-master.yaml

// в данном файле мы создаем таблицы --liquibase formatted sql : Определяет формат файла и позволет делать комиты
// во внутрение таблицы liquibase в лог которые автоматически создаются в постграссе все происходит в одной сессии
// каждая сессия отмечается : --changeset ashvedov:1 где ashvedov вносится в бд как автор а 1 номер сессии
db.changelog-1.0.sql


// в данном фай вносится данные в пределах сессии так же помечаются что от какой сесии надписью --changeset ashvedov:1
db.changelog-2.0.sql